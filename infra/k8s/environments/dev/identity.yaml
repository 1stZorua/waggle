keycloak:
  keycloak:
    replicaCount: 1

    auth:
      adminUser: admin
      adminPassword: admin

    extraEnvVars:
      - name: KC_HOSTNAME
        value: 'keycloak.waggle.local'

    ingressRoute:
      host: keycloak.waggle.local
