fluent-bit:
  enabled: true

  config:
    service: |
      [SERVICE]
          Daemon Off
          Flush 1
          Log_Level info
          Parsers_File /fluent-bit/etc/parsers.conf
          HTTP_Server On
          HTTP_Listen 0.0.0.0
          HTTP_Port 2020
          Health_Check On

    inputs: |
      [INPUT]
          Name tail
          Path /var/log/containers/*traefik*.log
          multiline.parser docker, cri
          Tag traefik
          Mem_Buf_Limit 5MB
          Skip_Long_Lines On
          Parser cri

    filters: |
      [FILTER]
          Name parser
          Match traefik
          Key_Name log
          Parser json
          Reserve_Data On
          Preserve_Key On

      [FILTER]
          Name modify
          Match traefik
          Add source traefik

    outputs: |
      [OUTPUT]
          Name http
          Match traefik
          Host crowdsec-service.security
          Port 7422
          URI /
          Format json
          Json_date_key time
          Json_date_format iso8601

  daemonSetVolumes:
    - name: varlog
      hostPath:
        path: /var/log
    - name: varlibdockercontainers
      hostPath:
        path: /var/lib/docker/containers

  daemonSetVolumeMounts:
    - name: varlog
      mountPath: /var/log
      readOnly: true
    - name: varlibdockercontainers
      mountPath: /var/lib/docker/containers
      readOnly: true

  serviceAccount:
    create: true

  rbac:
    create: true

  resources:
    limits:
      cpu: 200m
      memory: 128Mi
    requests:
      cpu: 100m
      memory: 64Mi
